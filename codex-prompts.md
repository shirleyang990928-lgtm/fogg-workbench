# Codex Prompts for Shirley Reply Sync

## 0. 先看不改
请分析这个项目，但先不要修改任何文件。告诉我这个项目的结构、主要功能、风险点、下一步建议。

## 1. 只改视觉
请只修改 `styles.css`，不要改 `index.html` 和 `app.js`。
目标：页面更清楚、更统一、更容易找内容。
保留：所有功能、所有文字内容、所有分类和按钮行为。
完成后请列出修改摘要和测试清单。

## 2. 只修 bug
我遇到的问题是：【贴你的问题】。
请只修这个 bug，不要顺手改视觉，不要新增功能。
完成后请说明原因、修改位置、测试方法。

## 3. 加一个小功能
请新增功能：【写清楚功能】。
限制：不要删除旧功能；不要改动无关视觉；不要重构整个项目。
完成后请列出改动文件和测试步骤。

## 4. 整理代码
请整理代码结构和注释，但不要改变页面效果或功能。
目标：让我这个新手能看懂哪里是数据、哪里是渲染、哪里是复制、哪里是日程管理。

## 5. 拆分 app.js
请把 `app.js` 进一步拆成：
- `data.js`：默认话术/日程资料
- `render.js`：页面渲染
- `storage.js`：保存、导入、导出
- `main.js`：事件绑定和初始化
要求：功能保持一致，页面效果保持一致，不删除任何内容。
